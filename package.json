{
  "name": "traveloop",
  "version": "0.1.0",
  "private": true,
  "description": "Traveloop mobile travel planning app with an Expo client and Express API.",
  "scripts": {
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix mobile run start\"",
    "dev:server": "npm --prefix server run dev",
    "dev:mobile": "npm --prefix mobile run start",
    "check": "npm --prefix server run check && npm --prefix mobile run check"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}

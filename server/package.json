{
  "name": "traveloop-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "check": "node --check src/index.js",
    "test": "node --test",
    "vercel-build": "npm run prisma:generate && npm run prisma:deploy",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init",
    "prisma:deploy": "prisma migrate deploy",
    "prisma:studio": "prisma studio",
    "db:backup:local": "node scripts/db/sync.js backup-local",
    "db:backup:hosted": "node scripts/db/sync.js backup-hosted",
    "db:sync:pull": "node scripts/db/sync.js pull"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "cloudinary": "^2.10.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^8.5.2",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "multer": "^2.1.1",
    "prisma": "^5.22.0",
    "uuid": "^11.0.3"
  },
  "engines": {
    "node": ">=20 <23"
  }
}
